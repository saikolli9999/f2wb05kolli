extends layout

block content
  h1= title
  p Welcome to #{title}
  p This is Index page
  button(type="button", class="btn btn-primary" onClick="doBump()") Press Me
  div(id="bump") Bump the count
  script. 
    var count=0;
    var bump=1;
    function doBump() {
      count=count+bump;
      bump=bump+1;
      document.getElementById("bump").innerHTML=`Count ${count} with bump ${bump}.`;
    }
  a(href="http://localhost:3000/engines") Engines